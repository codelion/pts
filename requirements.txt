torch>=2.0.0
transformers>=4.40.0
# transformers pins huggingface_hub<1.0. Leaving this unbounded lets pip install
# a 1.x that transformers then refuses to import at runtime -- which is exactly
# what the checked-in venv had happened to it.
huggingface_hub>=0.30.0,<1.0
datasets>=2.12.0
scikit-learn>=1.0.0
numpy>=1.20.0
tqdm>=4.65.0
accelerate
sentence-transformers>=2.2.0
math-verify[antlr4_13_2]>=0.1.0
pytest>=7.0
